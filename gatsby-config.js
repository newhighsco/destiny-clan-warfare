module.exports = {
  siteMetadata: {
    title: `Destiny Clan Wars`,
  },
  plugins: [
    `gatsby-plugin-react-helmet`,
    {
      resolve: `gatsby-plugin-stylus`,
      options: {
        use: [],
      },
    },
  ],
}
