.prose
  > * + *
    margin-top $spacing--default

  h2, h3
    font-weight bold
    margin-bottom ($spacing--default * -1)

    &:last-child
      margin-bottom 0

  a[href]
    &:hover
    &:focus
      text-decoration underline
