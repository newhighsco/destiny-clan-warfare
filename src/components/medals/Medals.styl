.medals
  text-align center

.medals__icon
  fill $color--black
  margin 0 $spacing--half $spacing--half
  width rem(45)

  &:hover
    transform scale(1.1)

  +width-min($mq--tablet)
    width rem(60)
