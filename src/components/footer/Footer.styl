.footer
  border-top 1px solid $color--eclipse
  margin-top $spacing--double
  padding $spacing--quadruple 0 $spacing--default
  position relative

  +width-min($mq--tablet)
    margin-top $spacing--treble
    padding-top $spacing--treble

  +width-min($mq--tablet--landscape)
    padding-top $spacing--default

.footer__list
  color $color--silver
  padding $spacing--default 0
  text-transform uppercase

  +width-min($mq--tablet)
    text-align right

.footer__item
  margin-right $spacing--default

  +width-min($mq--tablet)
    margin-right 0

    & + &
      margin-left $spacing--default

.footer__link
  display block
  font-size smaller
  letter-spacing 0.4em

  &:after
    content ''
    display block
    border-bottom 1px solid transparent
    margin-right 0.4em

  &[href]:focus
  &[href]:hover
    &:after
      border-color $color--terracotta

.footer__logo.logo-icon
  fill $color--silver
  margin auto
  // opacity 0.25
  position absolute
  left 0
  right 0
  top ($spacing--double * -1)
